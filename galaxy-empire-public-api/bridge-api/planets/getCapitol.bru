meta {
  name: getCapitol
  type: http
  seq: 2
}

post {
  url: {{uri}}:{{port}}/api/v1/user/create
  body: json
  auth: inherit
}

body:json {
  {
    "login": "bormon"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
