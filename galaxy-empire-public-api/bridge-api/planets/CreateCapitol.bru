meta {
  name: CreateCapitol
  type: http
  seq: 1
}

post {
  url: {{uri}}:{{port}}/api/v1/planet/capitol
  body: json
  auth: inherit
}

body:json {
  {
    "userID": "{{userID}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
