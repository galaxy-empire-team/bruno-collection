meta {
  name: GetFleet
  type: http
  seq: 4
}

get {
  url: {{uri}}:{{port}}/api/v1/planet/fleet
  body: json
  auth: inherit
}

body:json {
  {
    "planetID": "{{planetID}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
