meta {
  name: GetPlanetBuildings
  type: http
  seq: 7
}

get {
  url: {{uri}}:{{port}}/api/v1/planet/buildings
  body: json
  auth: inherit
}

body:json {
  {
    "planetID": "{{planetID}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
