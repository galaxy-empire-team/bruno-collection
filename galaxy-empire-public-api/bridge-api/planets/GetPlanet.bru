meta {
  name: GetPlanet
  type: http
  seq: 6
}

post {
  url: {{uri}}:{{port}}/api/v1/planet/capitol
  body: json
  auth: inherit
}

body:json {
  {
    "userID": "{{userID}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
