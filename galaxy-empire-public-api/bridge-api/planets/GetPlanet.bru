meta {
  name: GetPlanet
  type: http
  seq: 2
}

get {
  url: {{uri}}:{{port}}/api/v1/planet
  body: json
  auth: inherit
}

body:json {
  {
    "planetID": "{{planetID}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
