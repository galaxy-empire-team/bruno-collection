meta {
  name: GetAllUserPlanets
  type: http
  seq: 5
}

post {
  url: {{uri}}:{{port}}/api/v1/planet/all
  body: json
  auth: inherit
}

body:json {
  {
    "userID": "{{userID}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
