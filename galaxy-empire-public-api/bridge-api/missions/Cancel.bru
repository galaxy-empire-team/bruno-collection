meta {
  name: Cancel
  type: http
  seq: 7
}

post {
  url: {{uri}}:{{port}}/api/v1/mission/cancel
  body: json
  auth: inherit
}

body:json {
  {
    "id": 1
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
