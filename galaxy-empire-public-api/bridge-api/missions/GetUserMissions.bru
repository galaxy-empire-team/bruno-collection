meta {
  name: GetUserMissions
  type: http
  seq: 3
}

post {
  url: {{uri}}:{{port}}/api/v1/mission/all/get
  body: json
  auth: inherit
}

body:json {
  {
    "userID": "{{userID}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
