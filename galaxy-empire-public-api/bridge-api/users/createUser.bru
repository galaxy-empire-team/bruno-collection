meta {
  name: CreateUser
  type: http
  seq: 1
}

post {
  url: {{uri}}:{{port}}/api/v1/user/create
  body: json
  auth: inherit
}

body:json {
  {
    "login": "bormon"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
